#include<iostream>
#include<cstdio>
#include<cmath>
using namespace std;
int main()
{
	int i,x;       //x的幂次和自变量 
	float t,s;     //当前项和总值 
	cin>>x;        //输入x 
	i=0; t=1; s=1; //初始化 
	while(abs(t)>le*pow(10,-10))//精度符合要求？ 
	{
		i++;
		t=t*x/i;
		s+=t;
	}
	printf("%.4f",s);//输出 
	return 0;
}
